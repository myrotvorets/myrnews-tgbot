name: Docker Image CI (PR)

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2.3.4
      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag myrotvorets/myrnews-tgbot:$(date +%s)
